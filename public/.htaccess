#deny from all
<IfModule mod_rewrite.c>
	RewriteEngine On

	RewriteRule ^vs/public/?(.*)$ http://vs.localhost/$1 [R=301,L]

	RewriteCond %{REQUEST_FILENAME} !-f
	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteRule ^ index.php [QSA,L]
</IfModule>